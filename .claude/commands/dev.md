Start the local dev server and confirm it's running.

Use the `preview_start` tool with the name "portfolio" to start the Next.js dev server on port 3000. After starting, take a screenshot with `preview_screenshot` to confirm the site is live, then tell the user the local URL (http://localhost:3000).
